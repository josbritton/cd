# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/master/deployment-apps-v1.json
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: miniflux
    app.kubernetes.io/component: reader
    app.kubernetes.io/part-of: rss
  name: miniflux
  namespace: rss
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: miniflux
      app.kubernetes.io/component: reader
      app.kubernetes.io/part-of: rss
  template:
    metadata:
      labels:
        app.kubernetes.io/name: miniflux
        app.kubernetes.io/component: reader
        app.kubernetes.io/part-of: rss
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - topologyKey: kubernetes.io/hostname
            namespaces: []
            namespaceSelector: {}
            labelSelector:
              matchLabels:
                app.kubernetes.io/name: miniflux
                app.kubernetes.io/component: reader
                app.kubernetes.io/part-of: rss
            matchLabelKeys:
            - pod-template-hash
      containers:
      - name: miniflux
        image: docker.io/miniflux/miniflux:2.3.2@sha256:cd353a6af9ce663291f405eddef26fa0d567f486b521c5d34e3fb50e41938b9a
        env:
        - name: ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: miniflux-admin
              key: passwd
              optional: false
        - name: ADMIN_USERNAME
          valueFrom:
            secretKeyRef:
              name: miniflux-admin
              key: user
              optional: false
        - name: CREATE_ADMIN
          value: "1"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: miniflux-dsn
              key: connection-string
              optional: false
          # value: postgres://miniflux:secret@db/miniflux?sslmode=disable
        - name: LOG_LEVEL
          value: debug
        - name: RUN_MIGRATIONS
          value: "1"
        - name: BASE_URL
          value: https://rss.swifthomelab.net
        - name: FETCHER_ALLOW_PRIVATE_NETWORKS  # needed for rss-bridge on rss-bridge.rss.svc.cluster.local
          value: "1"
        - name: HTTP_CLIENT_TIMEOUT
          value: "60"
        - name: POLLING_FREQUENCY
          value: "10"
        - name: POLLING_SCHEDULER
          value: entry_frequency
        - name: SCHEDULER_ENTRY_FREQUENCY_MIN_INTERVAL
          value: "10"
        - name: SCHEDULER_ENTRY_FREQUENCY_MAX_INTERVAL
          value: "2880"
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        # - name: metrics
        #   ...
        livenessProbe: &probe
          exec:
            command: [/usr/bin/miniflux, -healthcheck, auto]
        readinessProbe: *probe
        startupProbe: *probe
        # resources:
        #   requests:
        #     cpu: ...
        #     memory: ...
        #   limits:
        #     memory: ...
